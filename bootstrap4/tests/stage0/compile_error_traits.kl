--- expected

# compiler output:
[compile_error_traits.kl] Compile error: Missing implementation of function `double(self: Self): T` of trait `Doubler<T>` at compile_error_traits.kl:19:1
# exit code:
# stderr:
# stdout:

/expected ---

trait Doubler<T>:
    fn double(self) T
end

struct Num:
    value i32
end

impl Doubler for Num:
    -- Expect a compile error because `double` is not implemented.
end

fn main():
    let num = Num { value: 5 }
    let doubled = num.double()
    print(doubled.value)
end