--- expected

# compiler output:
# exit code:
0
# stderr:
# stdout:
2
3
2
1
2

/expected ---

fn main():
    -- fixme: support generic functions (Vector.from)
    -- let v = Vector<i32>.from([1, 2, 3])
    let v = Vector<i32>.new()
    v.push(1)
    v.push(2)
    v.push(3)
    v.iter()
        .filter(fn(x i32) => return x > 1)
        .for_each(fn(x i32) => print(x))
    let new_v = v.iter()
        .filter(fn(x i32) => return x <= 2)
        .collect()
    print(new_v.len())
    print(new_v[0])
    print(new_v[1])
end
