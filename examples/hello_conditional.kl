-- expect.stdout {
-- It's true! Got the else.
-- } expect.stdout
-- expect.exit_code: 0

fn main() {
    if true => print("It's true! ")
    if false {
        print("This should not be reached.")
    } else {
        print("Got the else.")
    }
}
