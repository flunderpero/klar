-- expect.stdout {
-- 
-- } expect.stdout
-- expect.exit_code: 0

union StrIntBoolColor = Str | Int | Bool | RGB(Int, Int, Int) 

fn print_union(v StrIntBoolColor) {
    -- todo: Currently, we cannot do anything with union types.
}

fn main() {
    let str StrIntBoolColor = "str"
    mut color StrIntBoolColor = StrIntBoolColor.RGB(1, 2, 3)
    print_union(str)
    color = 42
    print_union(color)
}
